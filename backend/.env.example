PORT=5001

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=soil_irrigation

# Moisture percentage below which a zone is flagged for watering
MOISTURE_THRESHOLD=30

# How often (ms) the simulator generates a new reading per zone
SIMULATOR_INTERVAL_MS=5000
